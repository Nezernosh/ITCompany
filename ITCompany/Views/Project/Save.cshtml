@model ITCompany.Models.Project

@{
    ViewBag.Title = "Save";
    ViewBag.PageTitle = "Save project";
}


<form method="post" action="@Url.Action("Save")">
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.ID)

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="row">
        <div class="col offset-m3 s12 m6 l6">
            <h5>Project</h5>
            <div class="section white">
                <div class="row">
                    <div class="input-field col s12">
                        @Html.DropDownListFor(model => model.ClientID, null, new { @id = "title" } )
                        @Html.LabelFor(model => model.ClientID, htmlAttributes: new { @for = "title" })
                    </div>
                </div>

                <div class="row">
                    <div class="input-field col s12">
                        @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @id = "title" } })
                        @Html.LabelFor(model => model.Title, htmlAttributes: new { @for = "title" })
                    </div>
                </div>

                <div class="row">
                    <div class="input-field col s12">
                        @Html.EditorFor(model => model.Status, new { htmlAttributes = new { @id = "title" } })
                        @Html.LabelFor(model => model.Status, htmlAttributes: new { @for = "title" })
                    </div>
                </div>

                <div class="row">
                    <div class="input-field col s12">
                        @Html.EditorFor(model => model.Description, new { htmlAttributes = new { @id = "contact" } })
                        @Html.LabelFor(model => model.Description, htmlAttributes: new { @for = "contact" })
                    </div>
                </div>

                <div class="row">
                    <div class="input-field col s12">
                        @Html.EditorFor(model => model.Budget, new { htmlAttributes = new { @id = "contact" } })
                        @Html.LabelFor(model => model.Budget, htmlAttributes: new { @for = "contact" })
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col offset-m3 s12 m6 l12 center">
            <button type="submit" class="waves-effect waves-light blue btn margin-right">Save</button>
            <a href="@Url.Action("Index")" class="waves-effect waves-grey btn-flat">Back</a>
        </div>
    </div>
</form>

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>